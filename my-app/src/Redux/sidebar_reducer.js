
let initialState = {
    myProfile: 'My profile',
    dialogs: 'Dialogs',
    music: 'Music',
    news: 'News',
    settings: 'Settings',
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer